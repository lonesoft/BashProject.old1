{
    "name": "vendor-name/project-name",
    "description": "project description",
    "version": "0.0.0",
    "license": "MIT",
    "authors": [
        {
            "name": "name",
            "email": "name@example.com"
        }
    ],
    "require": {
        "lonesoft/bash-project": "0.1.*"
    },
    "scripts": {
        "post-package-install": [
            "type ./vendor/bin/project-install >/dev/null 2>&1 && ./vendor/bin/project-install; echo \"Bootstrap the project: ./project-bootstrap \" || exit 0"
        ]
    },
    "repositories": [
        {
            "type": "vcs",
            "url": "git@github.com:lonesoft/BashProject.git"
        }
    ]
}
